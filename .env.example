# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Email Service Configuration
# Options: 'resend', 'sendgrid', 'supabase', 'console'
# Use 'console' for development (logs to browser console)
# Use 'resend' or 'sendgrid' for production
VITE_EMAIL_SERVICE=console

# Email API Key (Required for resend/sendgrid)
# Get from: https://resend.com or https://sendgrid.com
VITE_EMAIL_API_KEY=

# Sender Email Address (Required for all services)
# Must be verified with your email provider
VITE_EMAIL_FROM=noreply@motorent.com

# Application URL (Used in email links)
# Development: http://localhost:5173
# Production: https://your-domain.com
VITE_APP_URL=http://localhost:5173

# Stripe Payment Configuration
# Get from: https://dashboard.stripe.com/apikeys
# Use test keys (pk_test_) for development
# Use live keys (pk_live_) for production
# IMPORTANT: Only use PUBLISHABLE key in frontend environment
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# ============================================
# DATABASE MIGRATIONS REQUIRED
# ============================================
# Run these SQL migrations in Supabase SQL Editor (in order):
# 1. supabase/migrations/001_initial_schema.sql (creates 8 main tables)
# 2. supabase/migrations/002_sample_data.sql (optional test data)
# 3. supabase/migrations/003-020_*.sql (other features)
# 4. supabase/migrations/021_otp_storage_table.sql (OTP persistence)
#
# Verify all tables created:
# SELECT table_name FROM information_schema.tables WHERE table_schema = 'public';

