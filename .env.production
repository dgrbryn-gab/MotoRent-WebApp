# MotoRent Production Environment Variables
# IMPORTANT: Never commit this file to version control
# Copy from .env.example and fill in actual production values

# ============================================
# SUPABASE CONFIGURATION (REQUIRED)
# ============================================
# Get these from: https://supabase.com/dashboard/project/[PROJECT_ID]/settings/api

VITE_SUPABASE_URL=https://your-production-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# STRIPE PAYMENT CONFIGURATION (REQUIRED)
# ============================================
# Get these from: https://dashboard.stripe.com/apikeys
# IMPORTANT: Only use LIVE keys in production (pk_live_* format)
# NEVER expose sk_live_ keys in frontend - use backend edge functions

VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your_publishable_key_here

# ============================================
# EMAIL SERVICE CONFIGURATION (REQUIRED)
# ============================================
# Choose ONE: 'resend' (recommended), 'sendgrid', 'console' (dev only)

VITE_EMAIL_SERVICE=resend

# Resend Configuration
# Get API key from: https://resend.com/api-keys
# Must match verified domain in Resend dashboard
VITE_EMAIL_API_KEY=re_live_your_resend_api_key

# SendGrid Configuration (if using SendGrid instead)
# Get API key from: https://app.sendgrid.com/settings/api_keys
# VITE_EMAIL_API_KEY=SG.your_sendgrid_api_key

# Sender email address (must be verified with your email provider)
# For Resend: Must be from your verified domain
# For SendGrid: Can be any verified sender
VITE_EMAIL_FROM=noreply@yourdomain.com

# ============================================
# APPLICATION URL CONFIGURATION (REQUIRED)
# ============================================
# Used in email links and redirects
# Must match your actual production domain

VITE_APP_URL=https://motorent.com

# ============================================
# OPTIONAL: Stripe Webhook Configuration
# ============================================
# These should NEVER be in frontend environment
# Handle webhooks on backend via Supabase edge functions
# Get webhook signing secret from: https://dashboard.stripe.com/webhooks

# STRIPE_WEBHOOK_SECRET=whsec_live_your_webhook_secret (BACKEND ONLY)

# ============================================
# NOTES FOR PRODUCTION
# ============================================
# 1. Create this file locally - NEVER commit to git
# 2. Set in deployment platform (Vercel/Netlify/etc environment variables)
# 3. Use strong, unique values - never share these keys
# 4. Rotate keys periodically for security
# 5. Monitor API usage and rate limits
# 6. Set up alerts for payment failures and errors
